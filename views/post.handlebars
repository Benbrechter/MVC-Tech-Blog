<div class="card"> 
        <div class="card-header">  
        <h2>{{post_title}}</h2>
        <h3>{{user.name}}</h3>
        </div>
        <div class="post-content">
        <p >{{post}}</p>
    </div>
    </div>

    {{#each comments}}
    {{!-- write the handlebars for showing the comments --}}
    {{/each}}

    {{!-- leave a comment form if logged in --}}
    {{!-- use conditional rendering if loggedIn --}}